import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ['cdn-icons-png.flaticon.com', 'https://unpkg.com', 'https://cdn-icons-png.flaticon.com',],
  },
};

export default nextConfig;
